# Настройка на домашнем компьютере

Памятка: как поднять всё то же самое дома. Идите строго по шагам сверху вниз.

---

## Шаг 1. Поставить базовые программы

Их git не переносит — это программы уровня системы, ставятся один раз.

| Программа | Где взять |
|-----------|-----------|
| **VS Code** (редактор) | https://code.visualstudio.com — кнопка Download |
| **Claude Code** | расширение внутри VS Code (ставится в Шаге 3 автоматически) |
| **Git** | https://git-scm.com/download/win — обычно уже стоит вместе с VS Code |
| **GitHub CLI** (`gh`) | https://cli.github.com — кнопка Download, либо см. Шаг 4 |

> Скачивание может занять время — это нормально, делается один раз.

**Быстрый способ поставить `gh`** — вместо скачивания вручную вставьте в PowerShell:
```
winget install --id GitHub.cli --source winget
```
После установки **закройте и откройте терминал заново** — иначе команда `gh` не найдётся.

---

## Шаг 2. Войти в GitHub (чтобы скачать проект)

1. Открыть VS Code.
2. Слева внизу нажать на значок **человечка** (Accounts) → **Sign in with GitHub**.
3. Откроется браузер — подтвердить вход в аккаунт **islantev-ops**.

---

## Шаг 3. Включить синхронизацию настроек (расширения приедут сами)

Это заменяет «долгую установку» — все расширения и настройки подтянутся автоматически.

**Если VS Code на русском** (у нас так):

1. **Ctrl+Shift+P** → напечатать `Синхронизация параметров` → выбрать
   **«Включение синхронизации параметров…»**
   *(или: шестерёнка ⚙️ слева внизу → тот же пункт)*
2. Оставить галочки как есть → подтвердить.
3. Выбрать вход через GitHub — **«Войти с учетной записью GitHub»**.
4. Откроется браузер → подтвердить вход под **islantev-ops**.

*(В английском VS Code те же пункты называются Turn on Settings Sync… и Sign in & Turn on.)*

После этого подтянутся расширения и настройки — среди прочего русский языковой
пакет и Claude Code.

> **Важно про превью md-файлов.** Отдельное расширение для этого **не нужно** —
> просмотр встроен в сам VS Code (компонент `markdown-language-features`).
> Открыть любой `.md` → **Ctrl+Shift+V** → превью откроется рядом.
>
> Расширения `ognile.markdown-ognile` («Markdown Ognile»), упомянутого в старой
> версии этой памятки, **не существует** — его нет ни в маркетплейсе, ни в данных
> синхронизации. Название было записано неверно.

> Если какого-то расширения вдруг нет — открыть «Расширения» (значок кубиков слева),
> вписать название и нажать **Install**. Это 30 секунд.

---

## Шаг 4. Войти в GitHub CLI (для сохранения работы, по желанию)

Нужно, только если будете сохранять изменения из командной строки.

1. Открыть терминал: в VS Code нажать **Ctrl+`** (клавиша «ё»), либо меню
   **Terminal → New Terminal**. Можно и обычный PowerShell (Windows → `powershell` → Enter).
   **Команду набирать в терминале, а не в чате Claude** — их легко перепутать.
2. Вставить и нажать Enter:
   ```
   gh auth login --web --git-protocol https
   ```
3. Появится код вида `1A2B-3C4D` → скопировать его → нажать Enter (откроется
   браузер) → вставить код → **Authorize**.
4. Дождаться в окне зелёной строки `Logged in as islantev-ops`.
5. **Обязательно!** Сразу выполнить вторую команду:
   ```
   gh auth setup-git
   ```
   Без неё вход в `gh` есть, но Git о нём **не знает** — и `git push` всё равно
   будет требовать пароль. Эта команда связывает их между собой.

**Проверка, что всё срослось** — выполнить в папке проекта:
```
git push --dry-run
```
Должно ответить `Everything up-to-date` (или показать, что готово к отправке)
и **не спросить пароль**. Ничего никуда при этом не отправится — это холостой прогон.

---

## Шаг 5. Скачать проект

1. В VS Code нажать **Ctrl+Shift+P** → напечатать `Git: Clone` → Enter.
2. Вставить адрес репозитория:
   ```
   https://github.com/islantev-ops/boost-12.git
   ```
3. Выбрать папку, куда сохранить (например Рабочий стол).
4. Когда скачается — нажать **Open** (открыть папку).

Готово — проект дома, всё как на рабочем компьютере.

---

## Как сохранять новую работу (чтобы не потерялась)

После того как что-то поменяли в проекте:

**Простой способ — прямо в VS Code:**
1. Слева нажать значок **веток** (Source Control, третий сверху).
2. В поле сверху написать короткое описание что сделали.
3. Нажать **Commit** → затем **Sync Changes** (или **Push**).

**Или одной командой в PowerShell** (находясь в папке проекта):
```
git add -A; git commit -m "что сделал"; git push
```

Изменения улетят на GitHub и будут доступны на любом компьютере.

---

### Быстрая шпаргалка

- Адрес проекта: **https://github.com/islantev-ops/boost-12**
- Аккаунт GitHub: **islantev-ops**
- Превью md-файлов: **Ctrl+Shift+V** — встроено в VS Code, ставить ничего не надо
- Если `git push` просит пароль → забыли `gh auth setup-git` (Шаг 4, пункт 5)
- Терминал в VS Code: **Ctrl+`** (клавиша «ё»)
